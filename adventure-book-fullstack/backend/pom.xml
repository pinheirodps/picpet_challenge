<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.4</version>
    <relativePath/>
  </parent>

  <groupId>com.pictet</groupId>
  <artifactId>adventure-book-backend</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>Adventure Book - Backend</name>
  <description>Spring Boot backend for the Adventure Book application (Pictet Technologies assessment)</description>

  <properties>
    <java.version>21</java.version>
    <springdoc.version>2.6.0</springdoc.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
          <!--
            By default repackaging replaces the plain jar with an executable fat jar, whose
            classes sit under BOOT-INF/classes where another project cannot compile against
            them. Giving the fat jar a classifier leaves the plain jar in place as the main
            artifact, so the sibling BDD suite can depend on this module's domain classes.
            Running the application is unaffected: `mvn spring-boot:run` is what the README
            documents, and the executable jar is still built, now as
            `adventure-book-backend-1.0.0-exec.jar`.
          -->
          <classifier>exec</classifier>
        </configuration>
      </plugin>

      <!--
        Coverage report, written to target/site/jacoco/index.html by `mvn test`. Reported
        rather than enforced: a build that fails on a percentage tends to produce tests
        written to raise the number instead of tests that would catch a defect.
      -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.12</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Builds the Angular application and packages it inside this jar, so the whole thing runs
      from one command with nothing but a JDK installed:

        ./mvnw -Pfullstack spring-boot:run     → http://localhost:8080

      Off by default, because it costs minutes and a network connection on every build while
      producing nothing a backend developer needs. Day to day the two halves run separately —
      `ng serve` gives the frontend hot reload, which a bundle copied into a jar cannot.

      frontend-maven-plugin downloads its own Node and npm into frontend/node/, so this does
      not need either on the PATH. The output lands in the jar's static/ directory, which
      Spring Boot serves from the application root.

      It installs with `npm ci` rather than `npm install`, so the build is reproducible — and
      so a package-lock.json that has drifted from package.json fails here rather than being
      silently papered over. That is a feature, but it does mean a dependency added by hand
      without regenerating the lock will break this profile while `npm start` carries on
      working.
    -->
    <profile>
      <id>fullstack</id>
      <build>
        <plugins>
          <plugin>
            <groupId>com.github.eirslett</groupId>
            <artifactId>frontend-maven-plugin</artifactId>
            <version>1.15.1</version>
            <configuration>
              <workingDirectory>../frontend</workingDirectory>
              <installDirectory>../frontend</installDirectory>
            </configuration>
            <executions>
              <execution>
                <id>install-node-and-npm</id>
                <goals>
                  <goal>install-node-and-npm</goal>
                </goals>
                <configuration>
                  <nodeVersion>v20.17.0</nodeVersion>
                </configuration>
              </execution>
              <execution>
                <id>npm-install</id>
                <goals>
                  <goal>npm</goal>
                </goals>
                <configuration>
                  <arguments>ci</arguments>
                </configuration>
              </execution>
              <execution>
                <id>npm-build</id>
                <goals>
                  <goal>npm</goal>
                </goals>
                <configuration>
                  <arguments>run build</arguments>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- The built bundle becomes the jar's static content. -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-resources-plugin</artifactId>
            <executions>
              <execution>
                <id>copy-frontend-bundle</id>
                <phase>prepare-package</phase>
                <goals>
                  <goal>copy-resources</goal>
                </goals>
                <configuration>
                  <outputDirectory>${project.build.outputDirectory}/static</outputDirectory>
                  <resources>
                    <resource>
                      <directory>../frontend/dist/frontend/browser</directory>
                    </resource>
                  </resources>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
